{
  "name": "apella-figma-export",
  "version": "1.0.0",
  "description": "Start a local server and export the app UI to a Figma file",
  "private": true,
  "scripts": {
    "start": "node scripts/serve.js",
    "figma-export": "node scripts/figma-export.js"
  },
  "dependencies": {
    "puppeteer": "^22.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
